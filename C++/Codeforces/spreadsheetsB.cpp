#include <bits/stdc++.h>
#define pb push_back
#define fi first
#define se second
typedef unsigned long long ul;
typedef long long ll;
using namespace std;

int main(){
    ios_base::sync_with_stdio(0); cin.tie(); cout.tie();

    ll t;
    string s;

    cin >> t;
    while(t--)
    {
        cin >> s;
        if (s[0] == 'R' && (s[1] - '0') % 10 == (s[1] - '0'))
        {

        }
        else
        {

        }
    }

    return 0;
}
